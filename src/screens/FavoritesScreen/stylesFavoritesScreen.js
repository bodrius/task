import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginHorizontal: 20,
    flex: 1,
  },
  input: {
    marginTop: 10,
    borderColor: 'red',
    borderWidth: 1,
    borderRadius: 13,
    height: 50,
    marginBottom: 50,
  },
});
